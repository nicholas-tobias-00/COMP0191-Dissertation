% ============================================================
% TODO before submission
% ============================================================
% CONSISTENCY PASS (2026-08-19, same day, after user asked "does everything
% make sense" + "AOA is to be taken out right?"):
%   - AOA REMOVED from Ch6 Limitations (paragraph + figure + copied image
%     deleted) -- confirmed with user this was always meant to be cut
%     project-wide (TODO.tex's own "locked-in decisions" list already said
%     so); it had been reintroduced two passes ago on the strength of the
%     comprehensive_results_matrix.md source, which conflicted with the
%     standing decision. Ch5's "AOA is not used" sentence is unaffected/
%     still correct.
%   - Fixed two dangling "Section 6.4.2" cross-references in Ch6 (its own
%     Interpretability section + a table caption) that broke when a new
%     subsection ("Why a Simplified, One-Shot Architecture") was inserted
%     earlier and shifted "Generation of the Simplified Scenario Model"
%     from 6.4.2 to 6.4.3 without updating the two references to it.
%   - Fixed a confusing "(Section 4.5.3, Chapter~\ref{Chap5})" citation in
%     Ch6 that named Chapter 5 while giving a Chapter-4-only section number
%     -- now cites both chapters' actual sections explicitly.
%   - Fixed STALE DLinear/LSTM R^2 numbers in Ch7 (-5.06/-1.36) left over
%     from an early draft, predating the comprehensive-matrix rewrite of
%     Ch5 -- now cites the current Ch5 Table 5.1 numbers (-1.751/-0.651)
%     and dropped a "Bi-LSTM" mislabel (Ch5's forecasting roster uses plain
%     LSTM; Bi-LSTM is Ch4-gap-filling-only).
%   - Fixed a Ch7 claim that Tower 2 has a "structural inability to support
%     uncertainty calibration" citing BOTH Ch4 4.5.4 and Ch5 5.3.1 -- wrong
%     on both counts: Ch4's gap-filling UQ table shows Tower 2 DOES
%     calibrate normally (PICP 0.941/0.918, n=12,384); the actual
%     uncalibratable-Tower-2 finding is forecasting-rollout-specific, in
%     Ch5 Section 5.3.5.2, not 5.3.1. Rewrote to correctly scope the claim
%     to forecasting only and cite the right subsection.
%   - Fixed Ch7's R^2-comparability citation for Tower 2 (was "Section 4.3",
%     which is the general R^2_OLS-vs-sklearn metric-definition caveat, not
%     Tower-2-specific -- the actual Tower-2 caveat is in Section 4.5.3.1).
%   - Fixed a grammar error in a Ch7 subsection heading ("Remains a
%     Difficult Tasks" -> "Remain Genuinely Difficult Tasks") introduced by
%     an external edit to the file between sessions.
%   - Fixed an UNSUBSTANTIATED claim in Ch8 ("Tower 2's cross-validation
%     redesign moved its R^2 by more than 17 points" -- this exact framing/
%     number was never established anywhere else in the drafted chapters,
%     traced to an outline bullet point that was carried into Ch8's prose
%     without ever being written up in Ch3/Ch4 first). Replaced with the
%     real, already-established Ch4 numbers instead: R-01's split-design
%     failure (R^2=-16.9, Section 4.4.3) vs. the corrected full-period
%     gap-CV champion (R^2=+0.576, Section 4.5.3.1) -- both numbers already
%     appear elsewhere in the document, so this is now fully self-
%     consistent and, if anything, a more dramatic number than the original
%     unsourced "17 points" claim.
%   - Added 12 missing abbreviations to the front-matter "List of
%     Abbreviations" that are used repeatedly in Ch3-6 but were never
%     defined there (ACF, PACF, CQR, GCM, MICE, MPIW, QRF, STL, TICA, UMAP,
%     VPD, WAPE) -- the list previously only covered terms from the
%     original Ch1/Ch2 draft.
%   - Wrote a REAL Abstract (0 Abstract.tex was still literal `\lipsum[5]`
%     placeholder Latin text -- the single most glaring "doesn't make
%     sense yet" item in the compiled PDF, now that every chapter has real
%     content). Follows the file's own pre-existing 6-point structural
%     guide (field -> gap -> idea -> methodology -> results -> positioning).
%     Cross-checked its own R^2/MASE numbers against Ch1's Contributions
%     section for consistency (0.40-0.58 primary range, 0.68 challenger
%     ceiling noted separately, matching Ch1 exactly rather than
%     introducing a third, different-looking number).
% Recompiles clean throughout this pass: 85 pages, zero undefined refs/
% citations/labels/errors. Systematic verification method used: extracted
% every \section/\subsection/\subsubsection heading from all 8 chapter
% files to build an exact numbering map per chapter, then grepped every
% typed (non-\ref) "Section X.Y" / "Chapter N" cross-reference in the
% document and checked each one against that map by hand -- this is how
% the 6.4.2/6.4.3 and Ch7 UQ-section bugs above were actually found, not
% general re-reading alone.
%
% STILL OPEN, not touched this pass (pre-existing, tracked below in the
% original restructure-era TODO block): Acknowledgements page blank,
% Abstract keywords still placeholder ("keyword 1 - keyword 2 - keyword
% 3"), Glossary.tex still has leftover robotics/vision terms unrelated to
% this project, "Faculty of Engineering" on the title page not confirmed
% against actual UCL AI4SD enrollment, Appendix B still empty.
%
% FIGURES PASS (2026-08-19, same day, after user request "embed figures from
% the latest documented results"): 18 figures copied from results/figures/
% and notebooks/03c_gap_filling_revisited/_figures/ into a new report/Figures/
% directory (self-contained for submission) and embedded across Ch3-6:
%   - Ch3 (6 figs): T2 timeseries, T2 availability heatmap, gap-length
%     distribution, hourly ACF/PACF, STL decomposition, D5 TICA/UMAP/t-SNE
%     consensus feature-selection stability.
%   - Ch4 (3 figs): MDS actual-vs-predicted scatter, champion gap-filling
%     chain (T4 2021), UQ calibration/sharpness reliability diagram.
%   - Ch5 (6 figs): example recursive-rollout chain (T4 2018, TabPFN), I-03
%     interpretability (overall ranking, per-tower top-8, species split),
%     U-04 fan chart (T4 2021), U-07 LSU-stratified CQR comparison.
%   - Ch6 (3 figs): livestock-density-ladder trajectory to 2050, grazing-
%     timing trajectory, AOA-flagged-% trend (Limitations).
% One figure (ch6_livestock_ladder_trajectory.png) has an honestly-flagged
% mismatch: its "literature ceiling" legend still reads 3.0 LSU/ha (the
% original synthesized estimate), not the corrected 2.5 LSU/ha value now
% used in the chapter's prose/table -- caption states this explicitly rather
% than silently using a figure that contradicts the text. If the source
% figure is regenerated against the corrected value before submission,
% swap it in and drop that caption caveat.
% Source images were selected by browsing results/figures/ and the gap-
% filling notebook's _figures/ (not _figures_opus/, which is an older set)
% directory listings, then visually reviewing each candidate before
% embedding -- not a blind copy of whatever existed. All 18 verified to
% render correctly within page margins (visual PNG spot-check via
% pdftoppm) after embedding. Recompiles clean: 84 pages, zero undefined
% refs/citations/errors.
%
% DEEP-DIVE PASS (2026-08-19, same day, after user feedback "tables overflow
% the page" + "models missing from roster"): Chapters 4-6 substantially
% expanded using report/Outlines/comprehensive_results_matrix.md, a curated
% numerical source sheet the user supplied that supersedes several numbers
% used in the first drafting pass below. Changes:
%   - Ch4: full 13-configuration roster now in one table (training-set mean,
%     MDS, RF met-only, MICE, HyperImpute, RFm champion, LightGBM, XGBoost,
%     TabPFN, TabICLv2 pooled+solo, SAITS, Bi-LSTM) -- was previously only
%     3-4 rows. Added gap-length robustness table, daily-aggregation
%     sensitivity, expanded feature-ablation prose, prediction-averaging
%     table, expanded UQ (n, corr(width,error), daily conformal table,
%     explicit "predates TabICLv2-solo champion" caveat).
%   - Ch5: full 11-model forecasting table (was ~5) -- corrected several
%     numbers that were stale from an older/differently-scoped BEST_RESULTS.md
%     pull (TabICLv2 BASE+ALL MASE was wrongly 0.871, now correctly 0.7385;
%     TFT/DLinear/LSTM numbers similarly corrected). Added gap-filled-target
%     reversal table (real numbers, not just the qualitative rule), feature-
%     set-sensitivity table, tower-level persistence-MASE note (clearly
%     flagged as non-comparable to the climatology convention), expanded UQ
%     to 3 tables (8-model first-stage, champion recalibration, spike/
%     livestock-tier CQR).
%   - Ch6: added the reduced-driver 11-model comparison (motivates why
%     TabICLv2/FX_A_SPECIES was chosen over trees -- RF collapses to
%     R^2=-7.05 under reduced drivers). Corrected the CMIP6 mismatch
%     direction (temperature too COOL and precipitation too WET, not "cooler
%     and drier" as first drafted -- direction matters, since ratio<1 means
%     a correction that shrinks precipitation). Livestock-ladder and
%     grazing/fertiliser numbers updated to the matrix's authoritative
%     figures (small changes, e.g. lit-ceiling row); the earlier "provisional
%     pending same-day fix" hedge on the literature-ceiling value is DROPPED
%     since the matrix presents it as final (2.5 LSU/ha, UK Countryside
%     Stewardship Annex 8). Added the AOA-flagged-percentage diagnostic and
%     an explicit "PI generated for >99% of rows = availability, not
%     coverage" clarification to Limitations, per the matrix's own
%     instruction that these diagnostics are part of the results, not
%     optional caveats.
% All wide tables (Ch4/5/6) now consistently use \footnotesize +
% \resizebox{\textwidth}{!}{...} (or \small for narrower ones) -- fixes the
% user's "tables overflow beyond the page" report. Verified by rendering
% every wide table to PNG via pdftoppm and visually inspecting; all fit
% cleanly within margins. One real LaTeX error was introduced and fixed
% during this pass (a table preamble column-count mismatch in Ch5's
% champion-recalibration table -- "Extra alignment tab" error, caught by
% the recompile, not left in the final file). Recompiles clean: 74 pages,
% zero undefined refs/citations, zero LaTeX errors; ~18 minor underfull/
% overfull hbox warnings remain, all small (<70pt, one large one is the
% PRE-EXISTING title-page header/footer logo image, unrelated to this pass).
% Not yet cross-checked against the matrix: Chapter 4's condensed RMSE/MAE/
% MBE narrative (the matrix's full 39-row error-metrics table was
% deliberately not reproduced in full -- summarised in prose instead to
% keep the chapter readable; revisit if the user wants the full table).
%
% MAJOR RESTRUCTURE (2026-08-19): report rebuilt from the Outlines/ folder's
% finalized chapter outlines. All 8 chapters now have real drafted prose
% (not stubs) -- 1 Introduction through 8 Conclusion. Compiles cleanly
% (62 pages, zero undefined refs/citations, zero LaTeX errors; a handful of
% minor underfull/overfull hbox warnings remain, all cosmetic table/line-
% break spacing, not content-affecting). This is a full first draft, ready
% for the user's revision pass ("keep going, I will revise after you're
% done" -- this pass is now complete).
%
% references.bib: merged with report/references_mendeley.bib (kept original
% clean entries, added ~13 new ones: TabICL/TabICLv2, Hoo et al. 2026
% TabPFN-TS, Hyndman 2006, Koenker 1978, Makridakis 2022, Montero-Manso 2021,
% Charteris 2021, Semenov 2025, SAITS, Weytjens 2025, Wang 2025). Fixed two
% duplicate-key bugs present in the Mendeley export (Purcell2023 used twice,
% Zhu2023 used twice) by not carrying those exact keys forward.
% STILL MISSING, not sourced anywhere in the repo: Wu et al. (SPACSYS),
% Hay et al. 2000 (delta/anomaly bias-correction method -- Chapter 6
% describes the method without citing it, since no source was found). Also
% still needed: a firmer citation for the NWFP-representativeness claim in
% Chapter 2 (currently supported by cardenas2022/charteris2021spatial/
% oulaid2025/partridge2024/fakeye2024, not a single dedicated source).
%
% Verified numbers, safe to use as-is:
%   - Forecasting headline: TabPFN+species, MASE_climatology = 0.7150,
%     R^2 (observed) = -0.0382 (confirmed against
%     results/b09_b16_climatology_mase_full_table.csv).
%   - Gap-filling champion/challenger tables (Ch4) -- RFm 0.576/0.404/0.426,
%     TabICL-solo 0.676/0.428/0.423, HyperImpute 0.509/0.336/0.354,
%     D-103's R2_OLS challenger table -- all confirmed against
%     BEST_RESULTS.md and notebooks/03c_gap_filling_revisited/.
%   - Gap-filling UQ table (QRF_RFm vs TabICLv2_quantile) -- confirmed
%     against notebooks/03c_gap_filling_revisited/_data/
%     conformal_calibration_summary.csv.
%   - Forecasting UQ table (TabPFN/TabICLv2 PICP/MPIW/pinball) -- confirmed
%     against BEST_RESULTS.md's U-04 entry.
%   - FCH4 target missingness -- computed directly from
%     data/Hourly/consolidated_hourly.csv: T4=56.5%, T9=75.0%, T2=88.5%.
%   - Baseline-replication numbers (Ch4.4, R-01/R-02/R-03 vs Irvin/Zhu/Kim)
%     -- confirmed against CONTEXT.md's replication tracker.
%   - Chapter 6's bias-correction factors table and livestock-ladder /
%     grazing / fertiliser result tables -- pulled directly from
%     results/s06_bias_correction_factors.csv,
%     results/s06_annual_ch4_{livestock_v2,grazing,fertilizer}_summary.csv
%     (generated 2026-08-18/19, the actual completed S-06 bias-corrected-
%     driver pipeline -- this superseded an earlier, incomplete
%     understanding of S-06's status reached earlier in this drafting
%     session; the full pipeline (src/features/build_bias_corrected_cmip6.py,
%     notebooks/07_scenario_analysis/s06_master_runner.py and siblings) was
%     completed and produced real results during this same session).
%
% NOT yet fully verified / explicitly flagged as provisional IN THE TEXT
% ITSELF (not just here) -- see the inline caveats in each chapter:
%   - Chapter 6's "literature ceiling" livestock-density tier: the annual
%     summary CSVs used for that table's percentages predate a late
%     same-day correction (results/s06_lit_ceil_fix.py, 2.5 LSU/ha
%     UK Countryside Stewardship Annex 8 figure replacing an earlier 3.0
%     synthesized estimate) whose corrected output had NOT yet been written
%     back to any results file as of this drafting pass (confirmed via file
%     timestamps -- the fix script predates any file it was meant to
%     update). The "half" and "own historical max" tiers are unaffected.
%     ACTION NEEDED: once s06_lit_ceil_fix.py has actually been run and its
%     corrected output propagated, re-pull the "literature ceiling" row of
%     Chapter 6's livestock-ladder table.
%   - Chapter 6.4.3's "cost of simplification" MASE delta (~0.7405->0.7588,
%     +2.47%) is carried from this project's own experiment log, not
%     independently re-verified in this drafting pass -- flagged as such
%     inline in the chapter text itself.
%   - Chapter 6.2 (Interpretability): a TabICLv2-specific permutation-
%     importance pass for the exact S-06 architecture used in Chapter 6 has
%     not been executed -- the chapter is honest about this gap inline,
%     grounding its livestock-centric design in Chapter 5's TabPFN-based
%     interpretability findings instead (a different model, explicitly
%     flagged as such). ACTION: if a TabICLv2-specific permutation-
%     importance run is completed before submission, Section 6.2 should be
%     updated to cite it directly rather than the cross-chapter grounding
%     currently used.
%
% Decisions locked in (do not re-litigate without direct instruction):
%   - Streamlit / digital-shadow-interface objective: NEVER mentioned
%     anywhere in this report. 5 objectives only, no 6th.
%   - Gap-filling metrics are HOURLY resolution throughout, not daily.
%   - Scenario chapter is built on the TabICLv2 + FX_A_SPECIES + one-shot
%     architecture (bias-corrected CMIP6 drivers) -- the old S01/S04
%     level-residual hybrid is fully superseded, kept only as a one-line
%     methodological contrast in Section 6.4.1, not as a worked example.
%   - No IPCC Tier 1 comparison anywhere.
%   - Title unchanged: "AI for Agriculture: Towards Digital Twins for
%     Methane Emissions Forecasting and Scenario Analysis."
%   - No "production" / "production-adopted" framing anywhere in Ch4 --
%     TabICLv2 is simply stated as the best result.
%   - AOA (Area-of-Applicability) is cut project-wide. PICP/MPIW is the
%     sole UQ vocabulary throughout Ch4-6.
%   - SVM and SAITS removed from Chapter 4's model roster (SAITS "for now").
%   - TabICL and TabICLv2 are the SAME tool, used for both gap-filling
%     (Ch4) and forecasting (Ch5) -- introduced once in Ch4, back-referenced
%     from Ch5/Ch6.
%
% Also still open (unchanged from before, see chat history for detail):
%   - Acknowledgements / dedication pages are blank
%   - Abstract keywords are still placeholder ("keyword 1 - keyword 2 - keyword 3")
%   - Abstract body itself still has placeholder text (0 Abstract.tex not
%     touched this pass -- Chapter drafting took priority)
%   - Glossary.tex entries are leftover robotics/vision terms (SLAM, RANSAC,
%     Gauss-Newton, etc.) from the original template -- irrelevant to this
%     project, not yet cleaned up
%   - "Faculty of Engineering" on the title page not yet double-checked
%     against actual UCL AI4SD enrollment
%   - No figures included anywhere in the report yet (all results are
%     tables only) -- a natural next pass once the user has reviewed this
%     draft's structure and content
%   - Appendix B (Supplementary Experiments) still an empty/commented-out
%     stub -- candidate content: SAITS, SVM, target-transform/hurdle-model
%     diagnostics, monthly-downscale, CV-vs-rollout HPO comparison
%   - A handful of minor underfull/overfull hbox warnings remain (cosmetic
%     line-break spacing only, largest is ~29pt after this pass's table-width
%     fixes) -- not content-affecting, lowest priority
% ============================================================
